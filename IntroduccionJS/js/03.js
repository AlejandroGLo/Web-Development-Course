// Const - We cant initialize a variable with no value

const product = "Gaming Headphones"; //Product is the name of the variable, and "Gaming Headphones" is the value 

let disponible; //Variable without value (cant with const)

product = true; //The value of the variable is changed to a boolean value

disponible = true; //The value of the variable is assigned

const product1 = "Computer",
    disponible1 = true,
    category = "Computers"; //Multiple variables are declared in the same line